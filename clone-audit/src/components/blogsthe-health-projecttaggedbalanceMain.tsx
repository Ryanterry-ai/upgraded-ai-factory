export function ContentSection() {
  return (
    <section className="py-12">
      <div className="container mx-auto px-4 max-w-4xl">
        <h2 className="text-2xl font-bold mb-4" style={{ color: "#2563eb" }}>
          THE HEALTH PROJECT Filter by
        </h2>
        <p className="text-gray-600 leading-relaxed">
          balance All balance diure6 diuresis homeostasis intestinal health liquid pre-workout preworkout protein supplementation supplements workout Share Balance - Homeostasis April 4, 2019 — NEED® Supplements Tagged: balance homeostasis var load = function () { if (document.querySelectorAll('.locksmith-manual-trigger').length > 0) { Locksmith.ping(); } Locksmith.util.on('submit', 'locksmith-resource-form', function (event) { event.preventDefault(); var data = Locksmith.util.serializeForm(event.target);
        </p>
      </div>
    </section>
  );
}
